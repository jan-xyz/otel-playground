receivers:
  otlp:
    protocols:
      grpc:
      http:

exporters:
  awsemf:
  awsxray:
  logging:

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [awsxray]
    metrics:
      receivers: [otlp]
      exporters: [awsemf, logging]
